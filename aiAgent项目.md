# 项目介绍：知识库问答助手

这份文档用开发者的视角，解释这个项目是怎么工作的。它面向不懂技术的人，所以我会尽量少用术语；必须出现的技术名词，会直接说明它在系统里负责什么。

## 这个项目解决什么问题

这个项目是一个“基于资料回答问题”的智能助手。

普通聊天机器人可能会直接凭模型自己的知识回答问题，但这容易出现两个问题：

- 它可能不知道我们公司的最新资料。
- 它可能说得很像真的，但实际并没有依据。

这个项目的目标是：先让管理员上传产品资料、制度文件、说明文档，然后用户提问时，系统先去这些资料里找相关内容，再把找到的内容交给 AI 整理成自然语言回答。

简单说，它不是让 AI 随便聊，而是让 AI 围绕我们给它的资料回答。

## 用户看到的流程

用户打开网页后，可以做几件事：

1. 选择产品线和版本，比如 `Alpha / v1`。
2. 管理员上传知识库文件，比如 PDF、Markdown、TXT。
3. 用户输入问题。
4. 系统返回回答，并显示它查到了哪些资料片段。

这样做的好处是，不同产品线、不同版本之间不会互相串数据。问 `Alpha v1` 的问题，就只查 `Alpha v1` 的资料。

## 一次提问在系统里发生了什么

从用户点击“发送”开始，系统内部大概走这条链路：

```text
浏览器页面
  ↓
前端应用
  ↓
后端服务
  ↓
知识库检索
  ↓
AI 整理回答
  ↓
返回页面展示
```

更具体一点：

1. 前端把用户的问题发给后端。
2. 后端确认用户选择的是哪个产品线、哪个版本。
3. 后端把问题转换成适合检索的形式。
4. 系统去 Qdrant 知识库里找相关资料片段。
5. 如果找到内容，就把这些资料交给 LangChain 和大模型处理。
6. 大模型根据资料组织回答。
7. 前端展示回答，同时展示“查询到的内容”。

## 每个技术组件负责什么

### React 前端

React 负责用户看到和操作的网页。

它处理这些事情：

- 输入问题
- 选择产品线和版本
- 上传文件
- 展示回答
- 展示资料来源
- 标记回答是 AI 生成，还是直接来自知识库

可以把 React 理解成这个系统的“前台窗口”。

### Vite

Vite 是前端开发服务器。

当前项目规定：

- 前端运行在 `8080` 端口
- 后端运行在 `9090` 端口

开发时，浏览器访问的是前端 `http://127.0.0.1:8080`。当前端需要请求后端接口时，Vite 会把请求转发到 `http://127.0.0.1:9090`。

### FastAPI 后端

FastAPI 是后端服务框架。

它负责接收前端请求，比如：

- 用户提问
- 管理员上传文档
- 查询已有知识库
- 检查服务是否正常

可以把 FastAPI 理解成这个系统的“调度中心”。前端不直接操作知识库，也不直接调用 AI，而是统一交给后端处理。

### Qdrant 知识库

Qdrant 是向量数据库。

普通数据库擅长按关键词查，比如“退款”两个字。但用户提问经常不是完全一样的文字，比如：

- “退款规则是什么？”
- “钱什么时候退回来？”
- “取消购买后资金怎么返还？”

这些问题字面不完全一样，但意思接近。向量数据库的作用，就是帮助系统按“语义相似度”找资料。

在这个项目里，Qdrant 存的是上传文档拆分后的片段，以及每个片段对应的向量。

### Embedding

Embedding 可以理解成“把文字变成数字指纹”。

人能直接理解文字，但数据库不懂“这句话和那句话意思相近”。所以系统会先把文本转换成一组数字，再用这些数字计算相似度。

项目里支持几种 embedding 方式：

- `local`：本地计算，不依赖网络，启动快，效果较基础。
- `huggingface`：适合中文检索，效果更好，但首次使用可能需要下载模型。
- `openai-compatible`：调用外部 embedding 服务。

开发时如果网络不好，建议先用 `local`，否则 HuggingFace 模型下载可能导致请求长时间不返回。

### LangChain

LangChain 是连接“资料检索”和“大模型生成”的工具层。

它不是大模型本身，而是帮我们组织大模型怎么工作。

在这个项目里，LangChain 主要负责：

- 把检索到的资料交给模型
- 约束模型优先使用知识库内容
- 对条款类问题做精确抽取
- 调用兼容 OpenAI 接口的大模型，比如 DeepSeek

可以把 LangChain 理解成“AI 工作流程编排器”。

### DeepSeek / OpenAI-compatible 模型

大模型负责把资料整理成自然语言回答。

项目使用的是 OpenAI-compatible 接口，也就是说，只要服务提供类似 OpenAI 的调用格式，就可以接入。当前配置里使用 DeepSeek 作为聊天模型。

大模型做的不是存资料，而是读后端检索出来的资料，然后生成更容易理解的回答。

## 为什么要先检索再生成

如果直接把问题交给 AI，它可能回答得很流畅，但不一定符合我们的资料。

所以项目采用 RAG 方式。

RAG 可以理解成：

```text
先查资料，再让 AI 根据资料回答
```

这样做有三个好处：

- 回答更贴近公司上传的资料。
- 可以显示资料来源，便于追溯。
- 不同产品线、不同版本可以隔离，减少答错版本的风险。

## 上传文档后发生了什么

管理员上传文件后，后端会做这些事情：

1. 读取文件内容。
2. 如果是 PDF，就把 PDF 里的文字抽出来。
3. 把长文档切成较小片段。
4. 给每个片段生成 embedding。
5. 把片段和它的 metadata 存进 Qdrant。

metadata 包括：

- 产品线
- 产品版本
- 文件名
- 文档 ID
- 片段序号
- 原始文本

这些信息让系统可以在回答时知道资料来自哪里。

## 提问时如何避免串库

每次查询都带着两个关键条件：

- `product_line`
- `product_version`

后端查询 Qdrant 时，会强制加上这两个过滤条件。

也就是说，用户选择 `Alpha / v1` 时，系统只查 `Alpha / v1` 的资料，不会拿 `Beta / v1` 或 `Alpha / v2` 的内容来回答。

## 当前系统的一个重要细节

如果使用 HuggingFace embedding，首次运行时可能会访问 HuggingFace 下载或检查模型文件。如果网络不稳定，请求会看起来像“一直没返回”。

解决方式有两个：

- 开发环境先使用 `local` embedding。
- 提前把 HuggingFace 模型下载到本机缓存。

目前为了让本地服务顺畅运行，可以采用：

```env
APP_EMBEDDING_PROVIDER=local
APP_ENABLE_AI_GENERATION=true
APP_MODEL_PROVIDER=openai-compatible
```

这样检索不会因为 HuggingFace 网络问题卡住，同时回答仍然可以经过 LangChain 和大模型处理。

## 最终效果

这个系统最终提供的是一套可控的知识库问答能力：

- 资料由管理员上传。
- 查询范围由产品线和版本控制。
- Qdrant 负责找资料。
- LangChain 负责组织 AI 工作流。
- 大模型负责生成自然语言回答。
- 前端展示回答和资料来源。

从开发者角度看，这不是一个单纯的聊天页面，而是一个“有资料依据、有版本隔离、有检索链路、有 AI 生成”的业务问答系统。

