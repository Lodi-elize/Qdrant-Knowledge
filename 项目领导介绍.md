# 知识库问答助手项目介绍

## 一、项目定位

本项目是一个面向产品资料、接口文档、制度文件的智能问答系统。

它的目标不是做一个普通聊天机器人，而是让 AI 基于公司上传的正式资料回答问题。用户选择对应的产品线和版本后，可以直接用自然语言提问，系统会先从知识库中检索相关资料，再由 AI 整理成清晰的回答，并保留资料来源，方便追溯。

简单来说：

> 先查公司资料，再让 AI 根据资料回答。

## 二、解决的问题

在日常业务中，产品资料、接口规范、制度文档通常存在几个问题：

1. 文档数量多，人工查找效率低。
2. 文档内容长，定位具体条目耗时。
3. 不同产品线、不同版本容易混淆。
4. 直接使用通用 AI 容易出现没有依据的回答。

本项目通过“知识库检索 + AI 总结”的方式，把资料查找和内容理解结合起来，降低人工查文档的成本。

## 三、核心能力

### 1. 文档上传

管理员可以上传产品资料、接口文档、FAQ、PDF、Markdown、TXT 等文件。

上传后，系统会自动解析文档内容，并按段落切分成适合检索的小片段，存入知识库。

### 2. 产品线和版本隔离

每份资料都会绑定：

- 产品线
- 产品版本
- 文件名称
- 文档 ID
- 文本片段编号

用户提问时必须选择产品线和版本。系统只会在对应范围内查找资料，避免把其他产品或其他版本的内容混进回答。

### 3. 自然语言问答

用户不需要记住文档目录，也不需要知道接口在哪一页，只要输入问题，例如：

- `F30016 的内容是什么？`
- `客户风险等级是否匹配接口怎么调用？`
- `某个产品的退款规则是什么？`

系统会自动检索相关片段，并生成回答。

### 4. 精准编号提取

对于类似 `F30016` 这种“字母 + 五位数字”的接口编号，系统已支持精准识别。

当用户询问 `F30016` 或 `F30016 的内容是什么` 时，系统会优先定位这个编号对应的接口段落，并截取到下一个接口编号之前，尽量避免回答中混入相邻接口内容。

### 5. 来源追溯

系统返回答案时，会同时保留命中的资料来源，包括文件名、产品线、版本和片段内容。

这有助于业务人员判断回答是否可靠，也方便后续回到原始资料核对。

## 四、用户使用流程

```text
打开网页
  -> 选择产品线和版本
  -> 输入问题
  -> 系统检索知识库
  -> AI 整理回答
  -> 页面展示答案和资料来源
```

管理员使用流程：

```text
登录管理入口
  -> 选择产品线和版本
  -> 上传资料文件
  -> 系统解析并写入知识库
  -> 用户即可基于新资料提问
```

## 五、项目当前数据情况

当前知识库中已有多个产品线的数据，其中 `Gamma / v1` 是主要测试资料之一，内容来自通华财富聚宝台分销机构接入技术规范。

该文档包含大量接口规范，系统可以围绕接口编号、接口名称、请求参数、响应参数等内容进行问答。

示例：

- `F30016`
- `F30016 的内容是什么`
- `客户风险等级与产品是否匹配查询接口有哪些参数`

## 六、少量技术说明

本项目采用的是 RAG 架构。

RAG 可以理解为：

> Retrieval Augmented Generation，即“检索增强生成”。

也就是先从公司资料中检索相关内容，再把检索结果交给 AI 生成回答。

### 前端

前端使用 React 和 Vite，负责页面展示、问题输入、资料上传和结果展示。

项目约定前端运行在：

```text
http://127.0.0.1:8080
```

### 后端

后端使用 FastAPI，负责接收前端请求、管理上传文件、调用知识库检索，并组织 AI 生成回答。

项目约定后端运行在：

```text
http://127.0.0.1:9090
```

### 向量数据库

知识库使用 Qdrant 存储。

它不是简单按关键词查找，而是把文本转换成数字向量后，按照语义相似度检索。这样即使用户的提问和原文不是完全一样，也有机会找到相关资料。

### AI 生成

系统支持 OpenAI-compatible 模型接口，可以接入 DeepSeek 等兼容服务。

AI 的职责是基于检索出来的资料组织答案，而不是脱离知识库自由发挥。

### LangChain

LangChain 用于组织 AI 调用流程，让模型优先读取系统检索到的资料，并按要求输出更适合用户阅读的答案。

## 七、项目价值

### 对业务人员

- 减少翻文档时间。
- 能快速定位接口、规则、参数说明。
- 回答带有资料来源，便于核对。

### 对管理人员

- 公司知识可以沉淀成统一入口。
- 不同产品和版本可以分开管理。
- 后续可以扩展到客服、运营、产品、实施等多个场景。

### 对技术团队

- 前后端分离，结构清晰。
- 知识库检索、AI 生成、资料上传职责分开。
- 后续可以继续扩展权限、日志、审计、批量导入、模型切换等能力。

## 八、适合的应用场景

本项目适合用于：

- 产品文档问答
- 接口规范查询
- 客服知识库
- 内部制度问答
- 实施交付资料查询
- 运维手册问答

尤其适合文档多、版本多、人工检索成本高的场景。

## 九、后续可扩展方向

1. 增加用户权限体系，区分管理员、普通用户和只读用户。
2. 增加问答日志，统计高频问题和未命中问题。
3. 增加文档更新记录，便于追踪资料版本变化。
4. 增加更细粒度的精准提取规则，例如接口编号、章节编号、表格字段。
5. 接入企业内部模型或私有化模型，提升数据安全性。
6. 增加后台管理页面，支持知识库可视化维护。

## 十、总结

这个项目本质上是一套“可追溯、可隔离、可扩展”的企业知识库问答系统。

它把公司已有资料变成可以直接问答的知识服务，既保留了资料依据，又利用 AI 提升了查找和理解效率。对于资料量大、接口复杂、版本频繁变化的业务场景，这类系统可以明显降低沟通和查询成本。
